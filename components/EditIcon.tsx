'use client'

import React from 'react';
import { Pencil } from '@phosphor-icons/react';

const EditIcon = () => (
   <button>
    <Pencil size={24} color='#0000FF' />
    </button>
);

export default EditIcon;
